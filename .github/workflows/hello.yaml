name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-20.04
    steps:
      - name: Echo the message
        run: echo "Hola mundo!"
      - name: Tell me the date
        run: date
      - run: ls -l
